name = "ukuva"
compatibility_date = "2026-06-14"

# Cloudflare Pages: the production site is served from public/.
# Everything outside public/ (design/, docs/) stays in the repo but is NOT deployed.
pages_build_output_dir = "public"
